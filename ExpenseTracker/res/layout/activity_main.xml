<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ca.ualberta.cs.expensetracker.MainActivity" >

    <Button
        android:id="@+id/createNewClaimButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/chooseExistingClaimButton"
        android:layout_alignLeft="@+id/chooseExistingClaimButton"
        android:layout_alignRight="@+id/chooseExistingClaimButton"
        android:layout_alignStart="@+id/chooseExistingClaimButton"
        android:layout_below="@+id/chooseExistingClaimButton"
        android:layout_marginTop="31dp"
        android:text="@string/create_new_claim" />

    <Button
        android:id="@+id/chooseExistingClaimButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="66dp"
        android:text="@string/choose_existing_claim" />

</RelativeLayout>
